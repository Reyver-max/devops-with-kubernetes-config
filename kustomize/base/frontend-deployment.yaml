apiVersion: apps/v1
kind: Deployment

metadata:
  namespace: project
  name: todo-app

spec:
  replicas: 1

  strategy:
    type: Recreate

  selector:
    matchLabels:
      app: todo-app

  template:
    metadata:
      labels:
        app: todo-app

    spec:
      volumes:
        - name: project-storage
          persistentVolumeClaim:
            claimName: shared-log-claim

      containers:
        - name: todo-app
          image: PROJECT/TODO_APP
          imagePullPolicy: Always

          ports:
            - containerPort: 3000

          readinessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 1

          livenessProbe:
            httpGet:
              path: /livez
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 2

          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"

          envFrom:
            - configMapRef:
                name: todo-config

          volumeMounts:
            - name: project-storage
              mountPath: /usr/src/app/files
